---
layout: default
title: Notes
description: Quick thoughts, learnings, and work-in-progress ideas
permalink: /notes/
---

.hero
  h1 {{ page.title }}
  p.lead {{ page.description }}

| {% if site.notes.size > 0 %}
.notes
  | {% for note in site.notes %}
  article.note-preview
    h2
      a(href="{{ note.url }}") {{ note.title }}
    p.date {{ note.date | date: "%Y-%m-%d" }}
    | {% if note.tags %}
    .tags
      | {% for tag in note.tags %}
      span.tag {{ tag }}
      | {% endfor %}
    | {% endif %}
  | {% endfor %}
| {% else %}
p.no-notes No notes yet.
| {% endif %}
